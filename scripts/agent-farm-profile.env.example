# 用法：复制为仓库根下 .agent-farm/profile.env（整个 .agent-farm 已在 .gitignore，勿提交）
#
# OpenCode 与各模型 CLI 使用相同的环境变量名；请填入与 Cursor 里「同一厂商、同一把密钥」
#（例如 Cursor Settings → 模型 / API Keys 里配置的 Anthropic，则在此设置 ANTHROPIC_API_KEY）。
#
# 调度脚本会在启动 worker 前 source 本文件，worker 子进程也会继承这些变量（与 process.env 合并）。

# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# OPENROUTER_API_KEY=
# GEMINI_API_KEY=
# GITHUB_TOKEN=
