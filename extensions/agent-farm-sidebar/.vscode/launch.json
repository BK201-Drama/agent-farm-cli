{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Agent Farm Sidebar",
      "type": "extensionHost",
      "request": "launch",
      "args": ["${workspaceFolder}/../.."],
      "outFiles": ["${workspaceFolder}/out/**/*.js"],
      "preLaunchTask": "build"
    }
  ]
}
